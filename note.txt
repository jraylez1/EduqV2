1. Phần phát âm trong các câu hỏi chưa làm
2. sau khi đăng xuất, vào phần mainLesson bị lỗi
3. sửa khóa học FreeQ
4. trong phần userInfo, chưa đổi được avatar


  // const base64 = await FileSystem.readAsStringAsync(fileMp3Uri, {
                //     encoding: FileSystem.EncodingType.Base64,
                // });

                // const recordingResult = await PronunciationStore.checkBase64(base64, 'Hello world', 9, 'uk');
                // console.log(recordingResult);

                // const playbackObject = new Audio.Sound();
                // await playbackObject.loadAsync({ uri: fileMp3Uri });
                // await playbackObject.playAsync();

                // const formData = new FormData();
                // formData.append('file', {
                //     uri: fileMp3Uri,
                //     type: 'audio/mpeg',
                //     name: fileName,
                // });
                // formData.append('expectedText', 'Hello');
                // formData.append('extension', 'mp3');

                // const serverRecordingUri = `https://eduq-qtest.hoctructuyentrans.com/pronunciation/file/uk/${fileName}`;

                // const response = await axios.post(serverRecordingUri, formData, {
                //     headers: {
                //         'Content-Type': 'multipart/form-data',
                //     },
                // });

                // console.log('Upload successful', response.data);